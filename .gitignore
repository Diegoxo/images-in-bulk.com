# --- FAITHFUL GITIGNORE FOR IMAGES-IN-BULK.COM ---

# 1. Local Secrets (NEVER upload to GitHub)
.env

# 2. Dependencies (Too large for GitHub, reconstruction via composer.json)
/vendor/

# 3. System & Logs (Prevention: these might not be visible now but could be created later)
*.log
error_log
.DS_Store
Thumbs.db
